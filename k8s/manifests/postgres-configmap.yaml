apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: iagocavalcante-blog
data:
  01-init-database.sql: |
    -- Initialize database for Iago Cavalcante Blog
    CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
    CREATE EXTENSION IF NOT EXISTS "pgcrypto";